"""server.src.account — host operations for the TAccount tier (account + CharacterManager children).

Per-Lua-object host ops that compose the engine substrate (server/src/engine/*). Client-side RE
counterpart: ghidraRE/output/flowmodels/client/taccount.md (Session/CharacterManager/CharacterHolder/
CSharedData). The account's per-Com behavior itself is the SHIPPED Lua the host runs.
"""
