"""server.src.char — host operations for the char object tier (Player/CStatusPlayer/User).

Per-Lua-object host ops that compose the engine substrate (server/src/engine/*). Client-side RE
counterpart: ghidraRE/output/flowmodels/client/{player,cstatusplayer,user}.md.
"""
