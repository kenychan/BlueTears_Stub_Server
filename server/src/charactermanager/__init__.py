"""server.src.charactermanager — host operations for the CharacterManager / TPlayerCache roster tier.

Per-Lua-object host ops that RUN the shipped server Lua (CharacterManager.lua / TPlayerCache.lua) over
the engine substrate (server/src/engine/*). Client-side RE counterpart:
ghidraRE/output/flowmodels/client/{charactermanager,tplayercache}.md.
"""
