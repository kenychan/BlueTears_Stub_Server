# CLAUDE.md — Blue Tears / Punch Monster: faithful offline server

The complete ruleset, auto-loaded every session — nothing critical lives in a file I might forget to open. **Route map (READ FIRST, anti-loop) = `NORTH_STAR.md`** — the proven route + the DEAD lanes (never re-walk) + the one real next step; if a thought contradicts it, re-read instead of re-deriving. Live state + current verdict/blocker/next-step = **`CODEX_LATEST_STATUS.md`** (top entry only; older entries are archive). Optional lookup detail (commands, paths, version notes): `PROJECT_RULES.md`.

## Stage (stable orientation)

Build the faithful offline server (the emulator) so the client's OWN mechanics construct + render the char — NOT by force-patching. Home = **`server/`**: `docs/` (architecture · milestones · decisions), `re/` (host RE), `src/` (modules — keystone order M-OBJ → M-REPL → M-CODEC), `tools/`. **Build loop:** build/extend a module → live-test `py server/tools/derisk_run.py` (runs the harness, prints a PASS/FAIL verdict) → iterate. The scripted `Res/gss_stub_server_v4.py` + NpSlayer are **TEST / RE tools, now LEGACY** — kept only as live wire/crypto/channel **transport** for live-testing; its server logic is migrating into `server/src` (don't grow the stub — add components in `server/`). **Milestones:** char row shows → enter game with a template char → char acts → …

## Read-first records — consult BEFORE acting/claiming; re-read after compaction/resume

**`NORTH_STAR.md` (route map + dead lanes — read FIRST)** · `CODEX_LATEST_STATUS.md` (live state, top only) · `server/docs/ARCHITECTURE_QUICKREF.md` (host plan at a glance → `architecture.md` detail) + `server/re/` (the host build) · `PROTOCOL_QUICKREF.md` + `ghidraRE/output/flow_model.json` (client-RE pointer-index + control-flow graph) · latest `SESSION_FINDINGS_<date>.md`.

## §0 Discipline — the ones I keep breaking; never skip

- **Motion is not progress — produce new ground truth or stop.** Before acting, ask what NEW, decision-changing result it yields toward a milestone. Re-establishing what's already known (records, prior sessions, this chat) yields none — don't, always check quick ref is an action you want to performed was already done before. Meta-work (docs, plans, handoffs, scheduling) must serve a concrete next action, never replace it; prefer the single decisive test over a "thorough" reconfirmation of what you already believe. A turn that's mostly artifacts with no new result = circling: stop, state the one blocker, then run the decisive test or surface the decision.
- **Verify before you claim.** Read the PRIMARY evidence THIS run — the live `TW/Bin/NpSlayer_exp.log` (or `py server/tools/derisk_run.py` for the verdict), the actual bytes, the function an exception came from — before naming any wall / state / result. Never pattern-match an exception code or one register dump to a prior finding. A stale log is not running the test; if you didn't generate it this run, say so.
- **Don't force or guess.** At an undocumented hard wall where the next move is guesswork, STOP and surface to the user. The faithful host drives the engine's OWN construction — do NOT force-patch engine self-gates (`obj+0x18`); they exist because the engine needs real objects.
- **Commit checkpoints (git).** Commit before any large/experimental edit and again when a step works. `Milestone:` for breakthroughs (mirror to `qqxj_server`); else `feat:` / `checkpoint:` / `fix:` so `git log --grep='^Milestone'` stays the clean breakthrough line. Never revert to an old commit to "restore good state" without diffing + backing up current work first.
- **Client Lua first; engine = Gamebryo.** Grep the pre-decompiled mirror `Res/tw_lua_decompiled/` (the REAL TW client; NOT `extracted_named_v4`) before grinding disassembly. `Ni*`/scene/mesh/body-load → the local `gamebryo-v3/`/`gamebryo-v2/` SDKs, not the binary.
- **Brainstorm at walls; consult records before acting; keep context fresh.** Zero online community — reason from the client + open-source engine refs. `PROTOCOL_QUICKREF`/`flow_model`/`server/docs` are READ-FIRST, not write-only. Checkpoint and compact by ~50% context; re-read the read-first records after.

## Operating rules

- **Safety & scope.** Read/write inside this project only; the SOLE external write target is `C:\Users\keny-\Downloads\qqxj_server` — the clean milestone mirror (breakthrough-reproducing artifacts only: host/stub code, hooks, curated logs, recipes — no dumps, caches, or client assets). **The mirror must be SELF-CONTAINED to reproduce each milestone: sync ALL current tools (hook/stub source + `.def` + build script, every resolver/analysis tool, the in-VM recipes, the harness, the built hook DLL + its hash) — never leave a stale tool there. `MILESTONES.md` is an append-only DIARY: never delete/rewrite old entries (a later entry annotates superseded ones).** Don't modify the client binary, patch memory, or install code hooks unless target + purpose + rollback + anti-protection risk are understood first; default proof = unmodified client + static RE + server-side logs; back up any DLL/EXE before patching. Preservation context (offline recovery of a discontinued game for personal nostalgia) — don't re-ask to justify.
- **Live-run ops.** Stall = `TW/Bin/NpSlayer_exp.log` growth/markers, NOT launcher/server silence — read its length/last-write/tail before calling the client frozen; on a real stall kill ONLY `NClient.exe` + `PunchMonster.exe`, keep the elevated worker. The game launches only with the persistent elevated worker up: **before any live run, check `Res/AdminLauncher/launcher_heartbeat.txt` is fresh (stale ~15s); if down, just run `Res/start_admin_launcher.ps1` to pop the UAC (once — don't loop/re-pop), then re-verify fresh** before the harness (`derisk_run.py` wraps it). Kill a lingering stub (port 5004) first. Use `py` (Conda). NpSlayer logs the rendered game + UI state; open a LIVE log-tail window at session start. **SELF-DIAGNOSIS — read the client's FULL native-log capacity EVERY run, and BEFORE RE'ing any wall (standing rule).** Keep all channels always-on, read all live, RE nothing before reading them for THAT failure. **FOUR channels:** **(1) Lua** → `TW/Bin/client_internal.log` (`CLIENTLOG_TAP` tees the client's own `RunLogLn_Service`/`print` + `IS_DEBUG` → session events, `[LUALOG]`, `[Error]`+traceback). **(2) Engine-native** → `TRANSITION_DBGSTR` in `NpSlayer_exp.log` (the engine's own `OutputDebugString`). **(3) Native call-tree + fault bytes** → `STACKCHAIN` (FPO-robust call chain; fires on every exception/teardown; works at EBP=0) + per-AV `ACCESS` (r/w + fault addr) + register/stack-pointed memory = the **malformed object bytes**; resolve `py Res/stubs/resolve_stackscan.py <log> --tag=TRANSITION_EXCEPTION` (slice to the crash block first — many exceptions/run). Replaces the client's STUBBED `GetNpCallStack`. **(4) C↔Lua GAP (always-on too)** → construct/object LOGIC lives in **Lua**, invoked by C signals and executing via Lua→C callbacks; a Lua branch decision (e.g. EmptyImpl-vs-DefaultImpl realize) is **SILENT on (1)–(3)** — no log/throw/`OutputDebugString`, it just returns a different value. To witness it, **wrap the C↔Lua boundary fns in the VM** (`Template`/`CreateDeferedTemplate`/`CreateCom`/`goLua_RegistObject`/…) so every C→Lua / Lua→C crossing + branch logs a `[GAP]` line. `LUA_QUICKREF.md` = the Lua flow map (keep it current). **MINE THE FULL CAPACITY — NpSlayer seats ARE hooks (intercept/inspect/modify/call any VA, no crash needed); use them ACTIVELY:** **absence of a log ≠ proof** — for a silent drop, plant an active seat at the dispatch/validation point; to recover **the SHAPE the client needs**, seat the CONSUMER + read a VALID instance's bytes (that IS the required shape, off the client's own data); **DIFFERENTIAL TRACE** = drive the client's OWN working construct AND our server's FAILING attempt through the SAME seats, then diff — it FINDS a server bug (wrong bytes/order/opcode → fix the server) or PROVES the wall architectural (diverge at an init-bound/never-built dispatch). **Either way you stop guessing.** Mine all channels + active seats before disassembly. See [[npslayer-stackscan-tracer]], [[tplayer-local-construct-late111]], `LUA_QUICKREF.md`.
- **Keep the record current.** After any RE walk, update `ghidraRE/output/flow_model.json` (logic/control-flow graph) + `protocol_schema_index.json` (wire/object dictionary); `CODEX_LATEST_STATUS.md` findings cite the `SESSION_FINDINGS_<date>` lines/tags holding the evidence; `PROTOCOL_QUICKREF.md` is the clean pointer-chain. Document repro steps so the logic-tree can be rebuilt.
- **RE method.** Client Lua first (the binary gives only byte format + gates; the Lua gives message/arg names, call order, the object/component model — don't recover from the binary what the Lua states plainly). Log BOTH sides (client bytes + exact server response) on sign-in/channel/master/crypto/disconnect; normal traffic may stay summarized. Targeted instrumentation + bounded runs over blind testing; simplest code that works.
- **Workflow.** Save Token: architect reserves its budget for hard architecture / protocol-binary analysis / decisions / judgment-bearing edits; delegate bounded, compact-result mechanical work to subagents (eg.parse a long log for markers, decompile a `.luo` + extract a field list, sweep `ghidraRE/output` for an xref, build/deploy + report `BUILD_EXIT`, run a bounded repro). Don't delegate true one-liners or work that needs live decision context. At each milestone: log + `Milestone:` commit + mirror, then drive the next. Keep caches/artifacts tidy.
